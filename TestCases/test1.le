{test3: program to test expressions containing operators.}
{Already tested: write statements and constants.}
begin

  {a := 0.0;
  b := 0.0;
  c := -1.0;
  d := -1.0;
  a := a - b + c - d;
  a := a - b + c;

  write(a);
  writeln;}

  write(-5.0);
  writeln;
  write(+4.0+3.0);
  writeln;
  write(1.0+2.0*(-6.0)-20.0/4.0);
  writeln;
  write(-0.5);
  writeln;
  write(-0.4-0.3);
  writeln;
  write(0.1/(0.25+0.05)+2.0e-1*(-6.0)-0.2/0.4);
  writeln;
end
